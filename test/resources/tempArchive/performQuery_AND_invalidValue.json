{
	"title": "AND invalid query (AND value is object instead of array)",
	"input": {
		"WHERE": {
			"AND": {}
		},
		"OPTIONS": {
			"COLUMNS": [
				"courses_avg",
				"courses_dept",
				"courses_id",
				"courses_instructor",
				"courses_title",
				"courses_pass",
				"courses_fail",
				"courses_audit",
				"courses_uuid",
				"courses_year"
			],
			"ORDER": "courses_avg"
		}
	},
	"errorExpected": true,
	"expected": "InsightError"
}
